import type { AppPathname } from "@/utils/routes";

export const indexableStaticRoutes: AppPathname[] = [
  "/",
  "/projects",
  "/availability",
  "/about",
  "/we-buy",
  "/contact",
  "/privacy",
  "/cookies",
  "/terms",
];
